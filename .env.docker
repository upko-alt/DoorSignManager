# E-Paper Dashboard - Docker Compose Environment Variables
# Copy this file to .env and fill in your actual values

# ============================================
# PostgreSQL Configuration
# ============================================
POSTGRES_DB=epaper_dashboard
POSTGRES_USER=epaper_user
POSTGRES_PASSWORD=your_secure_password_here  # REQUIRED: Change this!

# ============================================
# Session Configuration
# ============================================
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
SESSION_SECRET=your_very_long_random_string_here  # REQUIRED: Change this!

# ============================================
# Admin User (Auto-created on first Docker run)
# ============================================
ADMIN_USERNAME=admin
ADMIN_PASSWORD=admin123
ADMIN_EMAIL=

# ============================================
# SSL/HTTPS Configuration (Let's Encrypt)
# ============================================
# REQUIRED for HTTPS: Your domain name
# Example: dashboard.yourdomain.com or yourdomain.com
# For multiple domains: "domain1.com domain2.com" (space-separated)
DOMAIN=

# REQUIRED for HTTPS: Email for Let's Encrypt certificate notifications
# This email will receive expiry warnings (though auto-renewal handles renewals)
CERTBOT_EMAIL=

# ============================================
# Cookie Security
# ============================================
# Set to 'false' for HTTP, 'true' for HTTPS/SSL
COOKIE_SECURE=true

# ============================================
# Application Configuration
# ============================================
NODE_ENV=production
PORT=5000

# ============================================
# E-Paper Configuration - PER-USER IN DATABASE
# ============================================
# NOTE: E-paper URLs and API keys are now configured per-user
# through the admin dashboard, NOT through environment variables.
# 
# To configure e-paper integration:
# 1. Log in as admin
# 2. Go to User Management
# 3. Edit each user and fill in their e-paper settings:
#    - E-Paper Import URL (e.g., https://in.zivyobraz.eu/)
#    - E-Paper Export URL (e.g., https://out.zivyobraz.eu/)
#    - E-Paper API Key (e.g., oIiPcXQO)
#
# Each user can have different e-paper endpoints and credentials.
